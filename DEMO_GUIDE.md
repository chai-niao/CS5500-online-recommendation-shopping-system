# 学校网络演示 - 快速指南

## 🚀 演示前准备（3步骤）

### 1️⃣ 启用移动热点
- **Windows 11**: 设置 → 网络和Internet → 移动热点 → 启用
- 记住热点名称和密码，分享给其他观看者

### 2️⃣ 启动所有服务
在项目根目录运行：
```powershell
.\demo.ps1
```

脚本会：
- ✅ 自动获取你的本地IP
- ✅ 启动后端服务（Port 5000）
- ✅ 启动前端服务（Port 3000）
- ✅ 生成网络访问链接

### 3️⃣ 分享给观看者
等待30-60秒服务启动，然后告诉别人访问：
```
http://你的IP:3000
```

例如：`http://192.168.1.100:3000`

---

## 🔍 手动验证

**检查后端：**
```
http://你的IP:5000/api/health
```
应该返回 `{"status": "ok", ...}`

**检查前端：**
```
http://你的IP:3000
```
应该看到首页（需要60秒左右才能加载完）

---

## 🆘 常见问题

### Q: 别人看不到网站？
**解决：**
1. 确认他们连接到你的移动热点
2. 运行 `ipconfig` 确认本地IP地址
3. 检查Windows防火墙是否允许 3000/5000 端口
   - 搜索 "Windows Defender Firewall"
   - 选择 "允许应用通过防火墙"
   - 确保 Node.js 在列表中

### Q: 页面加载很慢？
1. 第一次加载需要编译React，可能需要1-2分钟
2. AI推荐功能需要调用本地模型，查看后台是否有错误
3. 检查 `http://IP:5000/api/health` 后端是否健康

### Q: 显示"无法连接到服务器"？
1. 检查是否运行了 `.\demo.ps1`
2. 检查端口是否在用：
   ```powershell
   netstat -ano | findstr "3000\|5000"
   ```
3. 杀掉占用端口的进程（如果有残留）：
   ```powershell
   Get-Process node | Stop-Process -Force
   ```

### Q: 结束演示如何关闭？
- 直接关闭两个 PowerShell 窗口
- 或在项目目录运行：
  ```powershell
  Get-Process node | Stop-Process -Force
  ```

---

## 📊 演示建议

**推荐演示功能：**
- 🏠 首页推荐系统 - 展示AI协作过滤
- 🔍 搜索功能 - 展示语义理解
- ❤️ 收藏列表 - 展示用户偏好
- 🛒 购物车 - 实时库存管理
- 💬 AI客服 - 与LLM交互

**测试场景：**
1. 多人同时浏览 - 检查性能
2. 搜索不同商品 - 展示推荐效果
3. 添加到购物车 - 演示协作过滤学习

---

## 🔌 技术细节

**后端修改：**
- 监听 `0.0.0.0` 而不是 `localhost` （允许外部访问）
- CORS 允许来自任何本地IP的请求

**前端修改：**
- 通过环境变量 `REACT_APP_API_URL` 动态配置API地址
- 默认使用你的本地IP（由 `demo.ps1` 设置）

**服务架构：**
```
┌─────────────────────────────────────────────┐
│        他人的设备 (同一WiFi)                 │
│    http://192.168.1.100:3000               │
└──────────────────┬──────────────────────────┘
                   │
                   │ (你的移动热点 WiFi)
                   │
┌──────────────────▼──────────────────────────┐
│         你的笔记本 (主机)                    │
│  ┌─────────────────────────────────────┐   │
│  │  Frontend (React)                   │   │
│  │  :3000                              │   │
│  └──────────────┬──────────────────────┘   │
│                 │                           │
│  ┌──────────────▼──────────────────────┐   │
│  │  Backend (Node.js/Express)          │   │
│  │  :5000                              │   │
│  └──────────────┬──────────────────────┘   │
│                 │                           │
│  ┌──────────────▼──────────────────────┐   │
│  │  Databases & ML Services            │   │
│  │  PostgreSQL, MongoDB, Python        │   │
│  │  :8001, :8002                       │   │
│  └─────────────────────────────────────┘   │
└─────────────────────────────────────────────┘
```

---

## ✅ 检查清单

演示前确认：
- [ ] 移动热点已启用
- [ ] 后端/前端都启动成功
- [ ] `http://本地IP:3000` 可以访问
- [ ] 数据库连接正常（查看健康检查）
- [ ] 其他设备已连接到你的WiFi

演示中注意：
- [ ] 网络延迟 - 可能需要1-2秒响应
- [ ] 模型推理 - 第一次调用可能较慢
- [ ] 并发用户 - 多人同时浏览性能良好

---

**祝演示顺利！🎉**
